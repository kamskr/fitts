name: exercises_repository
description: Flutter package used for managing exercises.
version: 1.0.0
publish_to: none

environment:
  sdk: ">=2.12.0 <3.1.0"

dependencies:
  api_client:
    path: ../api_client
  app_models:
    path: ../app_models
  authentication_client:
    path: ../authentication_client
  clock: ^1.1.0
  rxdart: ^0.27.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  mocktail: ^0.3.0
  test: ^1.21.4
  very_good_analysis: ^3.0.1
